modules:
  - type: rpm-ostree
    install:
      - cockpit
      - cockpit-ostree
      - cockpit-networkmanager
      - cockpit-sosreport
      - cockpit-files

  - type: systemd
    system:
      enabled:
        - cockpit.socket